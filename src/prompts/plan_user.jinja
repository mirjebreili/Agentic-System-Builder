USER GOAL (copy this EXACTLY to your plan's "goal" field)
{{ user_goal }}

ATOMIC SUBTASKS (you MUST address ALL of these in your plan)
{{ split_tasks }}

{% if system_elements and system_elements != "No existing system elements mentioned." %}
EXISTING SYSTEM ELEMENTS (use these in your node prompts)
{{ system_elements }}
{% endif %}

INSTRUCTIONS
Create {{K|default(3)}} alternative plans that:
- Have "goal" = the exact USER GOAL above
- Create 3-6 nodes with meaningful IDs  
- Each node addresses one or more subtasks (state which ones)
- Linear flow (node1 → node2 → node3 ...)
- Confidence 0.75-0.9 if all subtasks covered

Output ONLY a JSON array. No markdown fences. No extra text.
