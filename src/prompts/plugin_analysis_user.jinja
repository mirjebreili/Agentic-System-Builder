Please analyze this Persian question and determine the correct sequence of plugins needed to resolve it.

**Persian Question:** {{question}}

**Available Plugins:**
{% for plugin in plugins %}
- {{plugin}}
{% endfor %}

**Analysis Instructions:**
1. **Persian Translation**: Understand what the question is asking for in English
2. **Operation Type**: Identify if this needs:
   - Data reading from Atlas service (search/fetch operations)
   - Data aggregation/summation (مجموع calculations)
   - Key filtering by prefix (شروع می‌شن با pattern)
   - Project/namespace specific operations

3. **Key Pattern Recognition**: Look for:
   - Prefix patterns like "price_", "cost_", "expense_" in the question
   - Keywords: مجموع (sum), شروع (starts), مقادیر انتهایی (final values)
   - Data location: پروژه (project), فضای نام (namespace)

4. **Plugin Mapping**:
   - If data needs to be fetched → partDeltaPluginHttpBasedAtlasReadByKey
   - If numeric aggregation needed → membasedAtlasKeyStreamAggregator
   - If both reading and summing → Chain them: Read → Aggregate

**Required Output Format:**
```
HttpBasedAtlasReadByKey --> membasedAtlasKeyStreamAggregator
```

**Your Analysis:**
- **Question Understanding**: [What is being asked?]
- **Required Operations**: [What needs to be done?]
- **Plugin Sequence**: [Final answer in required format]